<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.green.to_do.TodoMapper">
	<resultMap id="todo" type="com.green.to_do.TodoDTO">
    <id column="TODO_NUM" property="todoNum" />
    <result column="TODO_TITLE" property="todoTitle" />
  </resultMap>
  <select id="getTodoList" resultMap="todo">
    SELECT
      TODO_NUM
      , TODO_TITLE
    FROM TODO
  </select>
  <!-- 
    할 일 추가 쿼리
    - id: TodoMapper 인터페이스의 addTodo 메서드와 매칭
    - 파라미터: todoTitle (String 타입)
    - 기능: TODO 테이블에 새로운 할 일을 추가
  -->
  <insert id="addTodo">
    INSERT INTO TODO (
      TODO_TITLE
    ) VALUES (
      #{todoTitle}
    )
  </insert>

  <!-- 
    할 일 삭제 쿼리
    - id: TodoMapper 인터페이스의 deleteTodo 메서드와 매칭
    - 파라미터: todoNum (int 타입)
    - 기능: TODO_NUM을 기준으로 해당 할 일을 삭제
    - WHERE 조건: TODO_NUM = #{todoNum} - 전달받은 할 일 번호와 일치하는 행만 삭제
  -->
  <delete id="deleteTodo">
    DELETE FROM TODO
    WHERE TODO_NUM = #{todoNum}
  </delete>
  <update id="updateTodo">
    UPDATE TODO
    SET
      TODO_TITLE = #{todoTitle}
    WHERE TODO_NUM = #{todoNum}
  </update>
</mapper>































