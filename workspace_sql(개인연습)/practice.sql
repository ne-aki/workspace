#이곳은 연습장입니다.
#ここは練習帳です。
CREATE TABLE asdf(
	abc INT PRIMARY KEY AUTO_INCREMENT,
	def VARCHAR(10),
	ghi DATETIME DEFAULT SYSDATE()
);

DROP TABLE asdf;

INSERT INTO asdf VALUES(98,'32','2020-12-12');

SELECT * FROM asdf;

INSERT INTO asdf(def)
VALUES ('これは日本語です。');

UPDATE asdf
SET
	def = 'こんにちは'
WHERE abc = 3;

ROLLBACK;
COMMIT;

CREATE TABLE R1 (
	학번 VARCHAR(20)
	, 학점수 INT
);

DROP TABLE r1;

INSERT INTO r1 VALUES (20202222, 20);

SELECT * FROM r1;

CREATE TABLE R2 (학번 VARCHAR(20), 과목번호 VARCHAR(20));

INSERT INTO r2 VALUES (20203333, 'CS300');

SELECT * FROM r2;

(SELECT 학번 FROM r1)
INTERSECT
(SELECT 학번 FROM r2);